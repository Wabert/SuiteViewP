[pytest]
addopts = -m "not integration and not live_db2 and not outlook and not performance"
markers =
    integration: exercises multiple components, external files, or persistence
    live_db2: requires a configured live DB2 connection
    outlook: requires the local Outlook desktop application and mailbox
    performance: benchmark or high-volume test excluded from normal regression runs
